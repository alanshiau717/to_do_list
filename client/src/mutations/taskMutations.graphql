mutation CreateTask($data: CreateTaskInput!) {
  createTask(data: $data) {
    id
    name
    due
    isDeleted
    done
  }
}

mutation ModifyTask($data: ModifyTaskInput!) {
  modifyTask(data: $data) {
    id
  }
}
