mutation CreateList($data: CreateListInput!) {
  createList(data: $data) {
    id
    name
    done
    isDeleted
  }
}

mutation ModifyList($data: ModifyListInput!) {
  modifyList(data: $data) {
    id
  }
}
